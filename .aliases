# General Unix stuff
alias l='ls -CFlh'
alias lsd='ls -alF | grep /$'

## size of folders
alias folders='find . -maxdepth 1 -type d -print | xargs du -sk | sort -rn'

# Ruby Stuff
alias gogogo='bundle exec foreman start'
alias be='bundle exec'
alias ber='bundle exec rspec'

# Git Aliases
alias gap='git add -p'
alias gnap='git add -N . && git add -p'
alias glp='git log -p'
alias glg='git log --graph --oneline --decorate --color --all'
alias gb='git branch'
alias gc='git commit -v'
alias gca='git commit -a -v'
alias gcl='git clean -f -d'
alias gd='git diff'
alias gdc='git diff --cached'
alias gdh='git diff HEAD'
alias gl='git pull'
alias glod='git log --oneline --decorate'
alias gp='git push'
alias gpr='git pull --rebase'
alias gst='git status'
alias gr='git rebase'
alias grc='git rebase --continue'
alias gra='git rebase --abort'
alias reset-authors='git commit --amend --reset-author -C HEAD'
alias prune='git remote prune origin'

# Functions

###
# Function that traverses through the filesystem.
# @param - Integer - Number of folders to go up the filesystem tree.
###
up() {
local d=""
limit=$1
for ((i=1; i <= limit; i++))
do
d=$d/..
done
d=$(echo $d | sed 's/^\///')
if [ -z "$d" ]; then
d=..
fi
cd $d
}

# Personal Aliases
alias clog="find log -name '*.log' -exec sh -c 'echo \"\" > {}' \;"
alias dev='cd ~/dev/'
